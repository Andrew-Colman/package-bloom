{
  "name": "cli",
  "packageManager": "yarn@4.1.1",
  "scripts": {
    "build": "tsup",
    "dev": "sucrase-node index.ts --watch --transforms typescript,imports",
    "format": "prettier --write .",
    "lint": "eslint . --ext .ts",
    "test": "jest"
  },
  "dependencies": {
    "@workspace/utils": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@workspace/tooling": "workspace:^",
    "sucrase": "^3.35.0",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  }
}
